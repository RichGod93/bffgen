{
  "name": "{{.ProjectName}}",
  "version": "1.0.0",
  "description": "Backend-for-Frontend service generated by bffgen",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "dev:watch": "nodemon --watch src --watch .env src/index.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:integration": "jest --testPathPattern=integration",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write 'src/**/*.js'",
    "format:check": "prettier --check 'src/**/*.js'",
    "validate": "npm run lint && npm run format:check && npm test",
    "test:aggregation": "jest --testPathPattern=aggregation",
    "test:utils": "jest --testPathPattern=utils",
    "cache:clear": "node scripts/clear-cache.js"
  },
  "keywords": ["bff", "backend-for-frontend", "api", "fastify"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "fastify": "^4.28.1",
    "@fastify/jwt": "^7.2.4",
    "@fastify/cookie": "^9.3.1",
    "@fastify/env": "^4.3.0",
    "@fastify/swagger": "^8.14.0",
    "@fastify/swagger-ui": "^3.0.0",
    "fastify-plugin": "^4.5.1",
    "pino": "^8.19.0",
    "pino-pretty": "^10.3.1",
    "dotenv": "^16.3.1"
  },
  "optionalDependencies": {
    "redis": "^4.6.11"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "nock": "^13.5.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

