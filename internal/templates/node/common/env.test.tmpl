# Test Environment Configuration
# Auto-generated by bffgen
# This file is used during test execution

# Environment
NODE_ENV=test

# Server Configuration
PORT=3000
HOST=localhost

# JWT Configuration (use test secrets)
JWT_SECRET=test-secret-key-do-not-use-in-production
JWT_EXPIRES_IN=1h

# Cookie Configuration
COOKIE_SECRET=test-cookie-secret

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=error

# Backend Service URLs (Mock backends for testing)
# Option 1: Use nock to mock HTTP calls (no real backends needed)
# Option 2: Point to test backend services running in Docker

{{range .BackendServices}}
{{.EnvKey}}_URL=http://localhost:{{.Port}}/api
{{.EnvKey}}_TIMEOUT=5000
{{.EnvKey}}_RETRIES=1
{{end}}

# Redis Configuration (Optional - use mock for tests)
# REDIS_URL=redis://localhost:6379/1

# Rate Limiting (Disabled in tests)
RATE_LIMIT_ENABLED=false

# Test-specific settings
TEST_TYPE=unit
# Set to 'integration' for integration tests with real backends

# Database (if applicable)
# DATABASE_URL=postgresql://test:test@localhost:5432/testdb

